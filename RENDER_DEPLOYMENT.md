# 🚀 Деплой WhatsApp бота на Render (100% Бесплатно)

## 📋 Что такое Render?

**Render** - это современная облачная платформа для деплоя приложений с **полностью бесплатным тарифом**:

✅ **Free Tier включает:**
- 750 часов/месяц для веб-сервисов (достаточно для 1 приложения 24/7)
- PostgreSQL база данных (90 дней хранения)
- Автоматический SSL (HTTPS)
- Постоянный URL
- Автоматический деплой из GitHub
- 512MB RAM, Shared CPU

⚠️ **Ограничение Free Tier:**
- Auto-sleep после 15 минут неактивности (просыпается за ~30 секунд при первом запросе)
- Для WhatsApp это не проблема - бот проснется когда придет сообщение

---

## 🎯 Быстрый старт (5 минут)

### Шаг 1: Создайте аккаунт на Render

1. Откройте https://render.com/
2. Нажмите **"Get Started for Free"**
3. Войдите через **GitHub** (самый простой способ)
4. Разрешите Render доступ к вашим репозиториям

---

### Шаг 2: Запушьте код в GitHub

Файлы конфигурации уже созданы. Теперь нужно отправить их в GitHub:

```powershell
# Добавьте новые файлы
git add render.yaml start-render.sh render-variables.txt RENDER_DEPLOYMENT.md

# Сделайте коммит
git commit -m "Add Render deployment configuration"

# Отправьте в GitHub
git push origin main
```

---

### Шаг 3: Создайте Blueprint на Render

1. **Откройте Render Dashboard**: https://dashboard.render.com/
2. Нажмите **"New +"** → **"Blueprint"**
3. Выберите **"Connect a repository"**
4. Найдите репозиторий **`ernatgulnisa/chatbotG`**
5. Нажмите **"Connect"**
6. Render автоматически обнаружит файл `render.yaml`
7. Нажмите **"Apply"**

**Что произойдет:**
- Render создаст 2 сервиса:
  - `chatbotg-web` - веб-приложение (FastAPI + React)
  - `chatbotg-db` - PostgreSQL база данных
- Автоматически установит зависимости
- Запустит миграции базы данных
- Запустит приложение

---

### Шаг 4: Добавьте переменные окружения

После создания Blueprint:

1. **Откройте веб-сервис**: Dashboard → `chatbotg-web`
2. Перейдите в **"Environment"**
3. **Добавьте следующие переменные:**

Откройте файл `render-variables.txt` и скопируйте:

```bash
SECRET_KEY=qwFf2Z9JCGGp4gSKbUpbFPdlPsnWPWMsznq9UVCU-4w
ENCRYPTION_KEY=8oRNERAA6pWOOcmaz1XE9EOZH6m_CmAkXJUVhby3pew
WHATSAPP_VERIFY_TOKEN=my_secure_token_12345
WHATSAPP_APP_SECRET=c22442c388ac4de6f1b3b4cc6faa8fd3
```

4. **Нажмите "Save Changes"** - Render автоматически перезапустит сервис

---

### Шаг 5: Получите URL вашего приложения

1. После успешного деплоя в **Dashboard → `chatbotg-web`** вверху увидите URL:
   ```
   https://chatbotg-web.onrender.com
   ```
   (ваш URL будет другим)

2. **Обновите WEBHOOK_URL и ALLOWED_ORIGINS:**
   - Вернитесь в **Environment**
   - Добавьте 2 переменные (замените URL на свой):
     ```bash
     WEBHOOK_URL=https://chatbotg-web.onrender.com/api/v1/webhooks/whatsapp
     ALLOWED_ORIGINS=https://chatbotg-web.onrender.com
     ```
   - Нажмите **"Save Changes"**

---

### Шаг 6: Настройте WhatsApp Webhook

1. **Откройте Meta for Developers**: https://developers.facebook.com/apps/
2. Выберите ваше приложение WhatsApp
3. Перейдите: **WhatsApp → Configuration → Webhook**
4. Нажмите **"Edit"**
5. **Callback URL**: `https://chatbotg-web.onrender.com/api/v1/webhooks/whatsapp`
   (замените на ваш URL)
6. **Verify Token**: `my_secure_token_12345`
7. Нажмите **"Verify and Save"**
8. **Subscribe to webhook fields**: Включите:
   - ✅ `messages`
   - ✅ `message_status`
9. Нажмите **"Subscribe"**

---

## 🧪 Тестирование

### 1. Проверьте деплой

Откройте в браузере ваш URL:
```
https://chatbotg-web.onrender.com
```

Должна открыться веб-страница бота.

### 2. Проверьте webhook

```powershell
curl https://chatbotg-web.onrender.com/api/v1/webhooks/whatsapp
```

Должен вернуть статус 200 OK.

### 3. Отправьте тестовое сообщение

Отправьте сообщение на ваш WhatsApp Business номер. Бот должен ответить.

### 4. Проверьте логи

Render Dashboard → `chatbotg-web` → **Logs** - увидите:
```
📦 Running database migrations...
🗄️ Initializing database...
🌐 Starting FastAPI server...
INFO: Uvicorn running on http://0.0.0.0:10000
```

---

## 📊 Мониторинг

### Просмотр логов в реальном времени

Render Dashboard → `chatbotg-web` → **Logs**

### Проверка статуса деплоя

Render Dashboard → `chatbotg-web` → **Events** - история всех деплоев

### Метрики использования

Render Dashboard → **Account Settings** → **Usage** - сколько часов использовано

---

## 🔄 Автоматический деплой

Render автоматически деплоит при каждом `git push`:

```powershell
# Внесли изменения в код
git add .
git commit -m "Update bot logic"
git push origin main

# Render автоматически:
# 1. Заметит новый коммит
# 2. Скачает код
# 3. Пересоберет приложение
# 4. Перезапустит сервис
```

---

## ⚙️ Управление сервисами

### Перезапуск вручную

Render Dashboard → `chatbotg-web` → **Manual Deploy** → **Deploy latest commit**

### Приостановка сервиса

Render Dashboard → `chatbotg-web` → **Settings** → **Suspend Service**

(Сервис не будет работать, но не потеряет данные)

### Возобновление сервиса

Render Dashboard → `chatbotg-web` → **Settings** → **Resume Service**

---

## 🆘 Решение проблем

### Деплой не удался

1. **Проверьте логи**: Dashboard → `chatbotg-web` → **Logs**
2. **Частая проблема**: не установлены зависимости
   - Убедитесь что `backend/requirements.txt` и `frontend/package.json` в репозитории
3. **Ошибка миграций**: 
   - Проверьте что PostgreSQL создан (`chatbotg-db`)
   - Убедитесь что `DATABASE_URL` правильно подключен

### Бот не отвечает на сообщения

1. **Проверьте webhook**:
   ```powershell
   curl https://YOUR-APP.onrender.com/api/v1/webhooks/whatsapp
   ```
2. **Проверьте переменные**:
   - `WHATSAPP_VERIFY_TOKEN` должен совпадать с токеном в Meta Dashboard
   - `WHATSAPP_APP_SECRET` правильно скопирован
3. **Проверьте логи**: Dashboard → Logs - должны быть запросы от WhatsApp

### Сервис "засыпает"

Free Tier автоматически усыпляет сервис после 15 минут без запросов.

**Решения:**
- Бот проснется когда придет сообщение (задержка ~30 сек первый раз)
- Для постоянной работы: upgrade to Paid Plan ($7/месяц)
- Или используйте cron job для "пинга" каждые 10 минут

---

## 💰 Стоимость

### Free Tier
- **Стоимость**: $0
- **Лимиты**: 750 часов/месяц (31 день × 24 часа = 744 часа)
- **Ограничения**: Auto-sleep после 15 минут неактивности

### Starter Plan
- **Стоимость**: $7/месяц
- **Преимущества**: Нет auto-sleep, больше RAM (512MB → 1GB)
- **Рекомендуется для**: Production WhatsApp ботов с высокой нагрузкой

---

## 📚 Полезные ссылки

- **Render Dashboard**: https://dashboard.render.com/
- **Документация Render**: https://render.com/docs
- **Free Tier лимиты**: https://render.com/docs/free
- **Blueprint spec**: https://render.com/docs/blueprint-spec
- **Meta WhatsApp API**: https://developers.facebook.com/docs/whatsapp/

---

## ✅ Чеклист деплоя

- [ ] Аккаунт на Render создан
- [ ] GitHub репозиторий подключен к Render
- [ ] Blueprint применен (`render.yaml`)
- [ ] PostgreSQL база данных создана (`chatbotg-db`)
- [ ] Веб-сервис создан (`chatbotg-web`)
- [ ] Переменные окружения добавлены (SECRET_KEY, WHATSAPP_*)
- [ ] WEBHOOK_URL и ALLOWED_ORIGINS обновлены с реальным URL
- [ ] Деплой завершен успешно (статус: **Live**)
- [ ] WhatsApp webhook настроен в Meta Dashboard
- [ ] Тестовое сообщение отправлено и получен ответ

---

## 🎉 Поздравляю!

Ваш WhatsApp бот теперь работает 24/7 на Render абсолютно **БЕСПЛАТНО**!

🔗 **URL вашего бота**: `https://chatbotg-web.onrender.com` (замените на свой)

📱 **WhatsApp Webhook**: Настроен и работает

💬 **Готово к приему сообщений!**
