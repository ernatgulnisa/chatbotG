# WhatsApp Integration - Quick Start

## ✅ Что реализовано

### Backend Services

1. **WhatsApp Service** (`backend/app/services/whatsapp.py`)

   - ✅ Отправка текстовых сообщений
   - ✅ Отправка template сообщений
   - ✅ Интерактивные кнопки (до 3 кнопок)
   - ✅ Интерактивные списки
   - ✅ Отправка медиа (изображения, видео, документы, аудио)
   - ✅ Загрузка файлов на Meta CDN
   - ✅ Пометка сообщений как прочитанных
   - ✅ Обработка входящих webhook событий

2. **Bot Processor** (`backend/app/services/bot_processor.py`)

   - ✅ Обработка сообщений через сценарии ботов
   - ✅ Поддержка 6 типов узлов:
     - Welcome (приветствие)
     - Message (текстовое сообщение)
     - Question (вопрос с сохранением ответа)
     - Buttons (кнопки выбора)
     - Condition (условия и ветвления)
     - Action (действия: сохранение в CRM, создание сделки, теги, передача человеку)
   - ✅ Автоматический переход между узлами
   - ✅ Сохранение состояния диалога
   - ✅ Передача диалога оператору (human takeover)

3. **Webhook Endpoint** (`backend/app/api/v1/endpoints/webhooks.py`)
   - ✅ Верификация webhook от Meta
   - ✅ Прием входящих сообщений
   - ✅ Обработка статусов сообщений (sent, delivered, read, failed)
   - ✅ Автоматический запуск bot processor
   - ✅ Background tasks для асинхронной обработки

### Frontend Pages

1. **WhatsApp Settings** (`frontend/src/pages/WhatsAppSettings.jsx`)

   - ✅ Управление WhatsApp номерами
   - ✅ Добавление/редактирование/удаление номеров
   - ✅ Активация/деактивация номеров
   - ✅ Шифрование токенов
   - ✅ Инструкции по настройке
   - ✅ Копирование Webhook URL

2. **Live Chat** (`frontend/src/pages/LiveChat.jsx`)
   - ✅ Список активных диалогов
   - ✅ Поиск по имени/номеру
   - ✅ Отправка/получение сообщений в реальном времени
   - ✅ WebSocket интеграция
   - ✅ Перехват диалога у бота
   - ✅ Архивирование диалогов
   - ✅ Индикация статуса бота

## 🚀 Быстрый старт

### 1. Настройка Meta for Developers

Следуйте подробной инструкции в **[WHATSAPP_SETUP.md](./WHATSAPP_SETUP.md)**

Кратко:

1. Создайте приложение на https://developers.facebook.com/
2. Добавьте продукт WhatsApp
3. Получите Phone Number ID и Access Token
4. Настройте Webhook

### 2. Настройка .env файла

```bash
# Backend .env
WHATSAPP_API_URL=https://graph.facebook.com/v18.0
WHATSAPP_VERIFY_TOKEN=your_secret_verify_token_12345
WHATSAPP_APP_SECRET=your_app_secret_from_meta

# Encryption key для токенов (32 символа)
ENCRYPTION_KEY=your-encryption-key-32-characters
```

### 3. Добавление номера через UI

1. Откройте http://localhost:3001/whatsapp
2. Нажмите "Добавить номер"
3. Заполните форму:
   ```
   Название: Салон красоты - основной
   Номер: +79001234567
   Phone Number ID: 123456789012345 (из Meta Dashboard)
   WABA ID: 987654321098765 (опционально)
   Access Token: EAAxxxxx... (будет зашифрован)
   Активировать: ✅
   ```
4. Сохраните

### 4. Настройка Webhook в Meta

**Callback URL:**

```
https://your-domain.com/api/v1/webhooks/whatsapp
```

**Verify Token:**

```
your_secret_verify_token_12345
```

**Subscribe to fields:**

- ✅ messages
- ✅ message_status

Для локальной разработки используйте **LocalTunnel**:

```bash
npx localtunnel --port 8000 --subdomain my-bot-name
# Используйте: https://my-bot-name.loca.lt/api/v1/webhooks/whatsapp
```

Или запустите готовый скрипт:
```powershell
.\start-with-localtunnel.ps1
```

### 5. Создание и подключение бота

1. Перейдите в **Bots** → **Create New Bot**
2. Выберите WhatsApp номер из списка
3. Активируйте бота
4. Откройте **Bot Builder**
5. Создайте сценарий:

```
[Welcome] → Привет! Я бот салона красоты
    ↓
[Buttons] → Что вас интересует?
    - Записаться на услугу
    - Узнать цены
    - Задать вопрос
    ↓
[Condition] → Если "Записаться"
    → [Question] → Как вас зовут?
    → [Question] → На какую дату?
    → [Action] → Create Deal
    → [Message] → Заявка принята! Менеджер свяжется с вами.
```

6. Сохраните сценарий

### 6. Тестирование

1. Отправьте сообщение на WhatsApp номер с вашего телефона
2. Бот должен автоматически ответить
3. Следуйте сценарию
4. Проверьте **Live Chat** - там будет история
5. Нажмите "Перехватить у бота" для ручного управления

## 📋 API Endpoints

### WhatsApp Numbers

```bash
# Получить список номеров
GET /api/v1/whatsapp/numbers

# Добавить номер
POST /api/v1/whatsapp/numbers
{
  "display_name": "Main Number",
  "phone_number": "+79001234567",
  "phone_number_id": "123456789012345",
  "whatsapp_business_account_id": "987654321098765",
  "access_token": "EAAxxxxx...",
  "is_active": true
}

# Обновить номер
PUT /api/v1/whatsapp/numbers/{id}

# Удалить номер
DELETE /api/v1/whatsapp/numbers/{id}

# Переключить активность
PATCH /api/v1/whatsapp/numbers/{id}/toggle
```

### Conversations & Messages

```bash
# Получить диалоги
GET /api/v1/conversations?status=active&limit=50

# Получить сообщения диалога
GET /api/v1/conversations/{id}/messages

# Отправить сообщение
POST /api/v1/conversations/{id}/messages
{
  "content": "Привет!",
  "message_type": "text"
}

# Перехватить диалог у бота
POST /api/v1/conversations/{id}/takeover

# Архивировать диалог
PATCH /api/v1/conversations/{id}
{
  "status": "closed"
}
```

### Webhook

```bash
# Верификация (GET)
GET /api/v1/webhooks/whatsapp?hub.mode=subscribe&hub.verify_token=TOKEN&hub.challenge=CHALLENGE

# Получение событий (POST)
POST /api/v1/webhooks/whatsapp
{
  "object": "whatsapp_business_account",
  "entry": [...]
}
```

## 🔧 Программное использование

### Отправка сообщения через WhatsApp Service

```python
from app.services.whatsapp import WhatsAppService, get_whatsapp_service
from app.core.database import SessionLocal

db = SessionLocal()

# Получить сервис для номера
whatsapp_service = await get_whatsapp_service(
    whatsapp_number_id=1,  # ID из БД
    db=db
)

# Отправить текст
await whatsapp_service.send_text_message(
    to="79001234567",  # Без +
    text="Привет! Это сообщение от бота."
)

# Отправить кнопки
await whatsapp_service.send_interactive_buttons(
    to="79001234567",
    body_text="Выберите опцию:",
    buttons=[
        {"id": "1", "title": "Опция 1"},
        {"id": "2", "title": "Опция 2"},
        {"id": "3", "title": "Опция 3"}
    ]
)

# Отправить изображение
await whatsapp_service.send_media_message(
    to="79001234567",
    media_type="image",
    media_link="https://example.com/image.jpg",
    caption="Посмотрите это изображение"
)
```

### Обработка входящих сообщений

```python
from app.services.bot_processor import BotProcessor

bot_processor = BotProcessor(db)

# Обработать сообщение через бота
success = await bot_processor.process_message(message)

if success:
    print("Сообщение обработано ботом")
else:
    print("Бот не активен, требуется оператор")
```

## 🎯 Типы узлов Bot Builder

### 1. Welcome Node

```json
{
  "type": "welcome",
  "data": {
    "message": "Добро пожаловать! Чем могу помочь?"
  }
}
```

### 2. Message Node

```json
{
  "type": "message",
  "data": {
    "message": "Вот информация о наших услугах..."
  }
}
```

### 3. Question Node

```json
{
  "type": "question",
  "data": {
    "question": "Как вас зовут?",
    "saveAs": "customer_name" // Сохранится в customer.metadata
  }
}
```

### 4. Buttons Node

```json
{
  "type": "buttons",
  "data": {
    "message": "Выберите категорию:",
    "buttons": [
      { "id": "1", "label": "Стрижка" },
      { "id": "2", "label": "Окрашивание" },
      { "id": "3", "label": "Уход" }
    ]
  }
}
```

### 5. Condition Node

```json
{
  "type": "condition",
  "data": {
    "conditionType": "contains", // contains, equals, starts_with, ends_with, regex
    "value": "запис" // Если в сообщении есть слово "запис"
  }
}
```

Edges условия:

- `label: "true"` → следующий узел если условие выполнено
- `label: "false"` → следующий узел если условие не выполнено

### 6. Action Node

```json
{
  "type": "action",
  "data": {
    "actionType": "save_to_crm", // save_to_crm, create_deal, assign_tag, human_takeover
    "field": "phone",
    "value": "{{user_input}}"
  }
}
```

**Action Types:**

- `save_to_crm` - сохранить данные в customer.metadata
- `create_deal` - создать сделку
- `assign_tag` - присвоить тег клиенту
- `human_takeover` - передать диалог оператору

## 🔍 Отладка

### Проверка логов Backend

```bash
# Windows
cd C:\Users\UserHome\Desktop\chatbotG\backend
tail -f nul  # Or check terminal output

# Должны видеть:
# WhatsApp Webhook received: {...}
# Message processed: 123 from +79001234567
```

### Проверка WebSocket подключения

Откройте DevTools в браузере → Network → WS:

```
Status: 101 Switching Protocols
Connection: Upgrade
Upgrade: websocket
```

### Тест webhook вручную

```bash
# Верификация
curl "http://localhost:8000/api/v1/webhooks/whatsapp?hub.mode=subscribe&hub.verify_token=your_secret_verify_token_12345&hub.challenge=test"

# Должен вернуть: test
```

## 📊 Мониторинг в Meta Dashboard

1. **WhatsApp → API Setup → Recent Messages**

   - Последние отправленные сообщения
   - Статусы доставки
   - Ошибки

2. **WhatsApp → API Setup → Webhooks**

   - История webhook вызовов
   - Статусы (Success/Failed)
   - Payload данных

3. **App Dashboard → Analytics**
   - Общая статистика сообщений
   - API usage
   - Error rates

## ⚠️ Важные моменты

1. **Шифрование токенов**: Access Token автоматически шифруется при сохранении через AES-256
2. **Тестовый режим**: Можно отправлять только на добавленные номера (до 5 номеров)
3. **24-часовое окно**: Можно отправлять свободные сообщения только в течение 24 часов после последнего сообщения клиента
4. **Template Messages**: Для отправки вне 24-часового окна нужны утверждённые шаблоны
5. **Rate Limits**: 80 сообщений в секунду для Cloud API

## 🚨 Troubleshooting

| Проблема              | Решение                                                              |
| --------------------- | -------------------------------------------------------------------- |
| Webhook не вызывается | Проверьте Verify Token, убедитесь что URL доступен извне (туннель)   |
| 403 при отправке      | Добавьте номер получателя в тестовый список Meta Dashboard           |
| Токен истёк           | Создайте постоянный токен (Never Expires)                            |
| Бот не отвечает       | Проверьте что бот активен и привязан к номеру                        |
| Сообщения дублируются | Убедитесь что webhook не подписан дважды                             |

## 📚 Дальнейшая разработка

- [ ] Media download (получение медиа от пользователей)
- [ ] Template message management в UI
- [ ] Button click tracking в analytics
- [ ] Auto-retry для failed messages
- [ ] Multi-agent support
- [ ] Customer segmentation для broadcasts
- [ ] A/B testing для bot scenarios

---

**Готово к работе!** 🎉

Документация: [WHATSAPP_SETUP.md](./WHATSAPP_SETUP.md)
