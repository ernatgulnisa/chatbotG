# 🚀 Быстрый старт

## Запуск приложения за 5 минут

### Шаг 1: Скопируйте переменные окружения

```powershell
cd c:\Users\UserHome\Desktop\chatbotG
Copy-Item .env.example .env
```

### Шаг 2: Запустите с Docker Compose

```powershell
docker-compose up -d
```

Подождите 30-60 секунд, пока все сервисы запустятся.

### Шаг 3: Откройте браузер

Перейдите на http://localhost:3000

### Шаг 4: Создайте аккаунт

1. Нажмите "Sign up"
2. Заполните форму регистрации
3. Войдите в систему

### Готово! 🎉

Теперь вы можете:

- Создавать ботов
- Управлять клиентами
- Вести переписку
- Создавать сделки
- Отправлять рассылки

## 📱 Подключение WhatsApp

### Для тестирования (без реального WhatsApp):

Приложение работает без подключения к WhatsApp API. Вы можете:

- Создавать ботов и сценарии
- Тестировать интерфейс
- Управлять клиентами

### Для продакшена (с реальным WhatsApp):

1. **Получите доступ к WhatsApp Business API:**

   - Meta (Facebook): https://developers.facebook.com/
   - Или используйте провайдера: Gupshup, 360dialog, Twilio

2. **Настройте переменные в .env:**

```env
WHATSAPP_API_URL=https://graph.facebook.com/v18.0
WHATSAPP_VERIFY_TOKEN=your-secret-token
WHATSAPP_APP_SECRET=your-app-secret
```

3. **Настройте Webhook:**

   - URL: `https://your-domain.com/api/v1/webhooks/whatsapp`
   - Verify Token: укажите токен из .env
   - Подпишитесь на события: `messages`

4. **Перезапустите сервисы:**

```powershell
docker-compose restart backend
```

## 🛠 Полезные команды

### Просмотр логов

```powershell
# Все сервисы
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Только frontend
docker-compose logs -f frontend
```

### Перезапуск сервисов

```powershell
# Все сервисы
docker-compose restart

# Конкретный сервис
docker-compose restart backend
```

### Остановка

```powershell
docker-compose down
```

### Полная очистка (включая базу данных)

```powershell
docker-compose down -v
```

## 📚 Дополнительная документация

- **Полная инструкция:** см. `INSTALLATION.md`
- **API документация:** см. `API.md`
- **Онлайн документация:** http://localhost:8000/docs

## 🐛 Проблемы?

### Порт уже занят

```powershell
# Проверьте, какие порты заняты
netstat -ano | findstr :3000
netstat -ano | findstr :8000

# Остановите процесс или измените порт в docker-compose.yml
```

### Backend не запускается

```powershell
# Проверьте логи
docker-compose logs backend

# Убедитесь, что PostgreSQL запущен
docker-compose ps
```

### Frontend не подключается к Backend

```powershell
# Проверьте, что backend работает
curl http://localhost:8000/health

# Если нет curl, откройте в браузере
start http://localhost:8000/health
```

## 💡 Советы

1. **Первый запуск может занять несколько минут** - Docker скачивает образы
2. **Используйте темную тему** - переключатель в правом верхнем углу
3. **Все данные сохраняются** - даже после перезапуска Docker
4. **Для разработки** - см. раздел "Development" в README.md

## 🎯 Что дальше?

1. ✅ Создайте свой первый бот
2. ✅ Настройте сценарии общения
3. ✅ Добавьте тестовых клиентов
4. ✅ Протестируйте live chat
5. ✅ Изучите аналитику

## 📞 Нужна помощь?

- 📖 Читайте документацию: `INSTALLATION.md`
- 🔍 Проверьте API: http://localhost:8000/docs
- 💬 Смотрите логи: `docker-compose logs -f`

---

**Удачи в использовании WhatsApp Bot & CRM Platform! 🚀**
