# 🌐 Методы подключения WhatsApp Webhook

## Доступные варианты туннелирования

### 1. LocalTunnel ⭐ РЕКОМЕНДУЕТСЯ

**Самый простой способ - работает сразу!**

```powershell
.\start-with-localtunnel.ps1
```

**Преимущества:**
- ✅ Не требует дополнительной настройки
- ✅ Бесплатно навсегда
- ✅ Публичный HTTPS URL
- ✅ Работает на Windows/Mac/Linux

**Недостатки:**
- ⚠️ URL может измениться если subdomain занят
- ⚠️ При первом посещении показывает предупреждение (нажмите Continue)

**Webhook URL:**
```
https://whatsappbot-XXXX.loca.lt/api/v1/webhooks/whatsapp
```

---

### 2. Cloudflare Tunnel - Постоянный URL

**Для продакшн использования**

```powershell
.\start-with-cloudflare.ps1
```

**Преимущества:**
- ✅ **Постоянный URL** - не меняется
- ✅ Бесплатно навсегда
- ✅ Быстрый (CDN Cloudflare)
- ✅ Windows Service (автозапуск)

**Недостатки:**
- ⚠️ Требует домен в Cloudflare
- ⚠️ Первоначальная настройка сложнее

**Документация:**
- [CLOUDFLARE_TUNNEL_SETUP.md](./CLOUDFLARE_TUNNEL_SETUP.md)

---

### 3. Custom Tunnel - Для продвинутых

**Свой туннель на Python**

Требует наличия сервера с публичным IP.

**Документация:**
- [tunnel/README.md](./tunnel/README.md)

---

## Сравнение методов

| Параметр | LocalTunnel | Cloudflare Tunnel | Custom Tunnel |
|----------|-------------|-------------------|---------------|
| Сложность | ⭐ Очень просто | ⭐⭐ Средне | ⭐⭐⭐ Сложно |
| Постоянный URL | ⚠️ Может меняться | ✅ Да | ✅ Да |
| Требования | Ничего | Домен | Сервер с IP |
| Скорость | Средняя | Быстрая (CDN) | Зависит от сервера |
| Стоимость | 🆓 Бесплатно | 🆓 Бесплатно | Стоимость сервера |

---

## Быстрый старт

### Для начинающих:

```powershell
.\start-with-localtunnel.ps1
```

Откроется 3 окна:
1. Backend (FastAPI)
2. Frontend (Vite)
3. LocalTunnel с вашим публичным URL

**Скопируйте URL из окна LocalTunnel и используйте в Meta Dashboard!**

---

### Для продакшн:

Настройте Cloudflare Tunnel один раз - URL больше не изменится.

См. [CLOUDFLARE_TUNNEL_SETUP.md](./CLOUDFLARE_TUNNEL_SETUP.md)

---

## Troubleshooting

### LocalTunnel показывает предупреждение

При первом посещении URL типа `https://xxx.loca.lt` показывается страница с предупреждением.

**Решение:** Нажмите кнопку "Continue" или "Click to Continue"

### Subdomain занят

```
Error: subdomain is already being used
```

**Решение:** LocalTunnel автоматически генерирует случайный subdomain при каждом запуске

### Туннель не подключается

1. Проверьте интернет соединение
2. Убедитесь что backend запущен на порту 8000
3. Перезапустите скрипт

---

## Что использовать?

**Для разработки и тестирования:**
→ LocalTunnel (`start-with-localtunnel.ps1`)

**Для продакшн использования:**
→ Cloudflare Tunnel (постоянный URL)

**Если есть свой сервер:**
→ Custom Tunnel (максимальный контроль)
