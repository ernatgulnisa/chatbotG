# 🔄 Автоматическое обновление .env файла

## Описание

Теперь при добавлении или обновлении WhatsApp номера через веб-интерфейс, система **автоматически обновляет** файл `.env` с актуальными данными WhatsApp API.

## Что обновляется автоматически

Когда вы добавляете WhatsApp номер через `http://localhost:3000/whatsapp`, автоматически обновляются:

- ✅ **WHATSAPP_ACCESS_TOKEN** - Токен доступа WhatsApp API
- ✅ **WHATSAPP_PHONE_NUMBER_ID** - ID телефонного номера
- ✅ **WHATSAPP_BUSINESS_ACCOUNT_ID** - ID бизнес-аккаунта (если указан)

## Как это работает

### 1. Добавление нового номера

1. Откройте `http://localhost:3000/whatsapp`
2. Нажмите кнопку **"Добавить номер"**
3. Заполните форму:

   - Display Name (например, "Support Line")
   - Phone Number (например, "+77471234567")
   - Phone Number ID (из Meta Dashboard)
   - Access Token (ваш токен EAAQfk...)
   - WABA ID (опционально)

4. Нажмите **"Сохранить"**

✨ **Автоматически:**

- Данные сохраняются в базу данных (зашифрованно)
- Файл `.env` обновляется с актуальными значениями
- Вы увидите в логах бэкенда: `✅ .env file updated with WhatsApp credentials`

### 2. Обновление существующего номера

1. Откройте `http://localhost:3000/whatsapp`
2. Нажмите **"Редактировать"** на нужном номере
3. Измените данные (например, новый токен)
4. Нажмите **"Сохранить"**

✨ **Автоматически:**

- Обновленные данные сохраняются
- Файл `.env` обновляется
- Старые значения заменяются новыми

## Пример .env файла после добавления номера

```env
# WhatsApp Business API (Meta)
WHATSAPP_API_URL=https://graph.facebook.com/v18.0
WHATSAPP_VERIFY_TOKEN=my_secure_verify_token_12345
WHATSAPP_APP_SECRET=your-app-secret
WHATSAPP_ACCESS_TOKEN=EAAQfkE8YZAsYBPy8shMAUQkBUiRJO4svWq2TzNH...
WHATSAPP_PHONE_NUMBER_ID=458596360698754
WHATSAPP_BUSINESS_ACCOUNT_ID=405966389296634
```

## Преимущества

### ✅ Нет необходимости вручную редактировать .env

Раньше нужно было:

1. Добавить номер через интерфейс
2. Открыть `.env` файл
3. Найти нужные переменные
4. Вставить токены вручную
5. Сохранить файл
6. Перезапустить сервер

### ✅ Теперь всё автоматически!

Просто добавьте номер через интерфейс - всё остальное система сделает сама.

### ✅ Всегда актуальные данные

При обновлении токена в интерфейсе, `.env` автоматически синхронизируется.

### ✅ Безопасность

- В базе данных токены хранятся **зашифрованными**
- В `.env` файле токены в открытом виде (для работы приложения)
- `.env` файл **не должен** попадать в Git (добавлен в `.gitignore`)

## Техническая реализация

### Файл: `backend/app/utils/env_manager.py`

Класс `EnvManager` управляет `.env` файлом:

```python
from app.utils.env_manager import env_manager

# Обновить токен
env_manager.update_whatsapp_token("EAAQfk...")

# Обновить Phone Number ID
env_manager.update_whatsapp_phone_number_id("458596360698754")

# Обновить WABA ID
env_manager.update_whatsapp_business_account_id("405966389296634")
```

### Автоматическое обновление в API

В файле `backend/app/api/v1/endpoints/whatsapp.py`:

```python
# При создании номера
@router.post("/numbers")
async def create_whatsapp_number(...):
    # ... сохранение в БД ...

    # Автоматическое обновление .env
    env_manager.update_whatsapp_token(number_data.api_token)
    env_manager.update_whatsapp_phone_number_id(number_data.phone_number_id)
    if number_data.waba_id:
        env_manager.update_whatsapp_business_account_id(number_data.waba_id)
```

## Логи

При успешном обновлении в терминале backend увидите:

```
✅ .env file updated with WhatsApp credentials
```

При ошибке:

```
⚠️ Warning: Failed to update .env file: [причина ошибки]
```

> **Примечание:** Даже если обновление `.env` файла не удалось, номер всё равно будет сохранён в базе данных. Приложение продолжит работать.

## Проверка

После добавления номера проверьте `.env`:

```powershell
Select-String -Path ".env" -Pattern "WHATSAPP_"
```

Должны увидеть:

```
.env:41:WHATSAPP_API_URL=https://graph.facebook.com/v18.0
.env:42:WHATSAPP_VERIFY_TOKEN=my_secure_verify_token_12345
.env:43:WHATSAPP_APP_SECRET=your-app-secret
.env:44:WHATSAPP_ACCESS_TOKEN=EAAQfkE8YZAsY...
.env:45:WHATSAPP_PHONE_NUMBER_ID=458596360698754
.env:46:WHATSAPP_BUSINESS_ACCOUNT_ID=405966389296634
```

## Часто задаваемые вопросы

### Q: Нужно ли перезапускать сервер после добавления номера?

**A:** Нет! Токены сохраняются в базе данных (зашифрованные) и используются напрямую оттуда. Обновление `.env` нужно для совместимости и резервного копирования.

### Q: Что если у меня несколько WhatsApp номеров?

**A:** В `.env` сохранится **последний добавленный** номер. Все номера хранятся в БД, а `.env` используется как дефолтное значение.

### Q: Безопасно ли хранить токены в .env?

**A:** `.env` файл **не должен** попадать в Git (проверьте `.gitignore`). Он используется только локально. В production используйте переменные окружения сервера.

### Q: Можно ли отключить автоматическое обновление .env?

**A:** Да, просто закомментируйте строки с `env_manager.update_*` в файле `backend/app/api/v1/endpoints/whatsapp.py`

## Совместимость

- ✅ Windows
- ✅ Linux
- ✅ macOS
- ✅ Docker (переменные окружения)

---

**Готово!** Теперь вам не нужно вручную редактировать `.env` файл при добавлении WhatsApp номеров. 🎉
