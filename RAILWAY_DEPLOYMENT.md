# 🚂 Деплой на Railway - Пошаговая инструкция

## ✅ Что такое Railway?

**Railway** - это современная платформа для деплоя приложений с автоматической настройкой инфраструктуры.

### Преимущества:
- ✅ **500 часов бесплатно в месяц** (~20 дней работы)
- ✅ **Постоянный URL**: `https://your-project.up.railway.app`
- ✅ **Автодеплой из GitHub** - при каждом коммите
- ✅ **PostgreSQL включена** бесплатно
- ✅ **Простая настройка** - через веб-интерфейс
- ✅ **Автоматический SSL** (HTTPS)

---

## 🚀 Быстрый старт (5 минут)

### Шаг 1: Регистрация на Railway

1. Откройте https://railway.app/
2. Нажмите **"Start a New Project"**
3. Войдите через **GitHub** (рекомендуется)
4. Подтвердите доступ к репозиториям

### Шаг 2: Загрузка проекта на GitHub

Если проект еще не на GitHub:

```powershell
# Инициализировать Git (если еще не сделано)
git init

# Добавить все файлы
git add .

# Создать коммит
git commit -m "Initial commit for Railway deployment"

# Создайте репозиторий на https://github.com/new
# Затем подключите его:
git remote add origin https://github.com/ваш-username/chatbotG.git
git branch -M main
git push -u origin main
```

### Шаг 3: Создание проекта на Railway

1. В Railway нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите ваш репозиторий `chatbotG`
4. Railway автоматически обнаружит настройки

### Шаг 4: Добавление PostgreSQL

1. В проекте нажмите **"+ New"** → **"Database"** → **"Add PostgreSQL"**
2. Railway автоматически создаст базу данных
3. Переменная `DATABASE_URL` будет добавлена автоматически

### Шаг 5: Настройка переменных окружения

В разделе **Variables** добавьте:

```env
# Backend Settings
DATABASE_URL=${DATABASE_URL}  # Автоматически от Railway
ENVIRONMENT=production

# Security
SECRET_KEY=ваш-секретный-ключ-минимум-32-символа
ENCRYPTION_KEY=ваш-ключ-шифрования-32-символа

# WhatsApp
WHATSAPP_API_URL=https://graph.facebook.com/v18.0
WHATSAPP_VERIFY_TOKEN=ваш-токен-верификации
WHATSAPP_APP_SECRET=ваш-app-secret-из-meta

# CORS
ALLOWED_ORIGINS=https://your-project.up.railway.app

# Webhook URL (будет получен после деплоя)
WEBHOOK_URL=https://your-project.up.railway.app/api/v1/webhooks/whatsapp
```

### Шаг 6: Генерация секретных ключей

Railway предоставляет встроенные генераторы:

```bash
# Или сгенерируйте локально:
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

Скопируйте результат в `SECRET_KEY` и `ENCRYPTION_KEY`

### Шаг 7: Деплой

1. Railway автоматически начнет деплой
2. Следите за логами в разделе **"Deployments"**
3. Дождитесь статуса ✅ **"Success"**

### Шаг 8: Получение URL

1. Откройте вкладку **"Settings"**
2. В разделе **"Domains"** нажмите **"Generate Domain"**
3. Скопируйте URL: `https://your-project.up.railway.app`
4. Обновите переменную `WEBHOOK_URL`:
   ```
   WEBHOOK_URL=https://your-project.up.railway.app/api/v1/webhooks/whatsapp
   ```

---

## 🔧 Настройка WhatsApp Webhook

### 1. Откройте Meta Dashboard
https://developers.facebook.com/apps/

### 2. Настройте Webhook

**Callback URL:**
```
https://your-project.up.railway.app/api/v1/webhooks/whatsapp
```

**Verify Token:**
```
ваш-токен-из-WHATSAPP_VERIFY_TOKEN
```

**Subscribe to fields:**
- ✅ messages
- ✅ message_status

### 3. Проверьте работу

Отправьте сообщение на WhatsApp номер - бот должен ответить.

---

## 📊 Мониторинг

### Логи
```
Railway Dashboard → Deployments → View Logs
```

### Метрики
```
Railway Dashboard → Metrics → CPU, Memory, Network
```

### База данных
```
Railway Dashboard → PostgreSQL → Connect
```

---

## 💰 Лимиты Free Tier

| Ресурс | Лимит |
|--------|-------|
| **Часы работы** | 500 часов/месяц (~20 дней) |
| **RAM** | 512 MB |
| **CPU** | Shared |
| **Хранилище** | 1 GB |
| **Трафик** | Без лимита |
| **Проекты** | Неограниченно |

### Оптимизация использования часов:

1. **Остановка в нерабочее время:**
   ```
   Railway Dashboard → Settings → Service → Sleep after inactivity
   ```

2. **Переход на Starter Plan ($5/мес):**
   - Убирает лимит часов
   - Больше ресурсов (до 8 GB RAM)

---

## 🔄 Автоматический деплой

Railway автоматически деплоит при каждом push в GitHub:

```powershell
# Внесите изменения в код
git add .
git commit -m "Update bot logic"
git push

# Railway автоматически обнаружит изменения и задеплоит
```

---

## 📁 Структура файлов для Railway

Проект уже настроен с этими файлами:

```
chatbotG/
├── railway.json           # Конфигурация Railway
├── nixpacks.toml         # Конфигурация сборки
├── Procfile              # Команды запуска
├── start-railway.sh      # Скрипт запуска
├── backend/
│   ├── requirements.txt  # Python зависимости
│   ├── alembic.ini      # Миграции БД
│   └── app/
└── frontend/
    ├── package.json     # Node.js зависимости
    └── vite.config.js
```

---

## 🐛 Решение проблем

### Проблема 1: Деплой не запускается

**Решение:**
```
Railway Dashboard → Deployments → View Logs
```
Проверьте ошибки в логах.

### Проблема 2: База данных не подключается

**Решение:**
```
Railway Dashboard → PostgreSQL → Variables
```
Убедитесь что `DATABASE_URL` добавлена в основной сервис.

### Проблема 3: Webhook не работает

**Решение:**
1. Проверьте что `WEBHOOK_URL` правильно настроен
2. Проверьте `WHATSAPP_VERIFY_TOKEN` совпадает с Meta
3. Проверьте логи: `Railway Dashboard → Logs`

### Проблема 4: 500 часов кончились

**Решение:**
- Подождите до следующего месяца
- Или перейдите на Starter Plan ($5/мес)
- Или используйте другой аккаунт

---

## 💡 Советы по оптимизации

### 1. Используйте Production mode
```env
ENVIRONMENT=production
```

### 2. Включите сжатие
```python
# backend/app/main.py
from fastapi.middleware.gzip import GZipMiddleware
app.add_middleware(GZipMiddleware, minimum_size=1000)
```

### 3. Используйте кеширование
Railway включает Redis - используйте его для кеша.

### 4. Мониторьте использование
```
Railway Dashboard → Metrics → Usage
```

---

## 🚀 Переход на платный план

Когда бизнес вырастет:

### Starter Plan ($5/мес)
- Неограниченные часы
- 8 GB RAM
- 8 vCPU
- Приоритетная поддержка

### Как обновить:
```
Railway Dashboard → Settings → Billing → Upgrade to Starter
```

---

## 📚 Полезные ссылки

- **Railway Docs**: https://docs.railway.app/
- **Railway Discord**: https://discord.gg/railway
- **Railway Status**: https://status.railway.app/
- **Примеры проектов**: https://railway.app/templates

---

## ✅ Чеклист деплоя

- [ ] Создан репозиторий на GitHub
- [ ] Код загружен в GitHub
- [ ] Создан проект на Railway
- [ ] Добавлена PostgreSQL база
- [ ] Настроены переменные окружения
- [ ] Сгенерированы SECRET_KEY и ENCRYPTION_KEY
- [ ] Получен URL от Railway
- [ ] Обновлен WEBHOOK_URL
- [ ] Настроен Webhook в Meta Dashboard
- [ ] Протестирован бот (отправка сообщений)
- [ ] Проверены логи на ошибки

---

## 🎉 Готово!

Ваш WhatsApp бот теперь работает на Railway с постоянным URL:
```
https://your-project.up.railway.app
```

Webhook URL для Meta:
```
https://your-project.up.railway.app/api/v1/webhooks/whatsapp
```

**Следующие шаги:**
1. Протестируйте бота
2. Настройте автодеплой
3. Мониторьте метрики
4. При необходимости обновите до Starter Plan

---

**Поддержка:**
- Railway Discord: https://discord.gg/railway
- GitHub Issues: https://github.com/your-repo/issues
