# Тестирование API

## 🧪 Способы тестирования

### 1. Swagger UI (Рекомендуется)

Откройте в браузере: **http://localhost:8000/docs**

#### Тестирование через Swagger:

**Шаг 1: Регистрация пользователя**

1. Разверните `POST /api/v1/auth/register`
2. Нажмите "Try it out"
3. Введите данные:

```json
{
  "email": "test@example.com",
  "password": "TestPassword123!",
  "full_name": "Test User",
  "business_name": "My Business"
}
```

4. Нажмите "Execute"
5. Проверьте ответ (должен быть 201 Created)

**Шаг 2: Вход в систему**

1. Разверните `POST /api/v1/auth/login`
2. Нажмите "Try it out"
3. Введите:
   - **username**: `test@example.com`
   - **password**: `TestPassword123!`
4. Нажмите "Execute"
5. Скопируйте `access_token` из ответа

**Шаг 3: Авторизация в Swagger**

1. Нажмите кнопку "Authorize" 🔓 вверху страницы
2. Введите: `Bearer YOUR_ACCESS_TOKEN`
3. Нажмите "Authorize" и "Close"

**Шаг 4: Тестирование защищенных endpoints**

1. `GET /api/v1/auth/me` - получить свой профиль
2. `GET /api/v1/bots/` - список ботов (будет пуст)
3. `POST /api/v1/bots/` - создать бота (потребуется WhatsApp номер)

### 2. cURL (Командная строка)

```bash
# Health Check
curl http://localhost:8000/api/v1/health

# Регистрация
curl -X POST "http://localhost:8000/api/v1/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "TestPassword123!",
    "full_name": "Test User",
    "business_name": "My Business"
  }'

# Вход
curl -X POST "http://localhost:8000/api/v1/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=test@example.com&password=TestPassword123!"

# Получение профиля (замените TOKEN на ваш токен)
curl -X GET "http://localhost:8000/api/v1/auth/me" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"

# Список ботов
curl -X GET "http://localhost:8000/api/v1/bots/" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

### 3. Python скрипт

Запустите готовый тест-скрипт:

```bash
python test_api.py
```

**Примечание**: Скрипт автоматически:

- Проверит доступность API
- Зарегистрирует тестового пользователя
- Выполнит вход
- Протестирует основные endpoints
- Проверит авторизацию

### 4. Frontend приложение

Откройте: **http://localhost:3001**

1. Зарегистрируйтесь через UI
2. Войдите в систему
3. Откройте DevTools (F12)
4. Перейдите на вкладку "Network"
5. Взаимодействуйте с UI и наблюдайте API вызовы

## 📋 Доступные Endpoints

### Аутентификация (`/api/v1/auth`)

- `POST /register` - регистрация нового пользователя
- `POST /login` - вход в систему
- `POST /refresh` - обновление токена
- `GET /me` - получить текущего пользователя
- `POST /google` - вход через Google

### Боты (`/api/v1/bots`)

- `GET /` - список всех ботов
- `POST /` - создать бота
- `GET /{bot_id}` - получить бота
- `PUT /{bot_id}` - обновить бота
- `DELETE /{bot_id}` - удалить бота
- `POST /{bot_id}/toggle` - включить/выключить бота

### Сценарии (`/api/v1/bots/{bot_id}/scenarios`)

- `GET /` - список сценариев
- `POST /` - создать сценарий
- `GET /{scenario_id}` - получить сценарий
- `PUT /{scenario_id}` - обновить сценарий
- `DELETE /{scenario_id}` - удалить сценарий

### Клиенты (`/api/v1/customers`)

- `GET /` - список клиентов
- `POST /` - добавить клиента
- `GET /{customer_id}` - получить клиента
- `PUT /{customer_id}` - обновить клиента

### Диалоги (`/api/v1/conversations`)

- `GET /` - список диалогов
- `GET /{conversation_id}` - получить диалог
- `GET /{conversation_id}/messages` - сообщения

### Сделки (`/api/v1/deals`)

- `GET /` - список сделок
- `POST /` - создать сделку
- `GET /{deal_id}` - получить сделку
- `PUT /{deal_id}` - обновить сделку

### Рассылки (`/api/v1/broadcasts`)

- `GET /` - список рассылок
- `POST /` - создать рассылку
- `GET /{broadcast_id}` - получить рассылку
- `POST /{broadcast_id}/send` - запустить рассылку

## 🔐 Аутентификация

Все endpoints (кроме `/auth/register` и `/auth/login`) требуют JWT токен.

**Заголовок:**

```
Authorization: Bearer YOUR_ACCESS_TOKEN
```

**Получение токена:**

1. Зарегистрируйтесь или войдите
2. Скопируйте `access_token` из ответа
3. Используйте в заголовке Authorization

**Срок действия токена:**

- Access Token: 30 минут
- Refresh Token: 7 дней

## ✅ Ожидаемые результаты

### Успешная регистрация:

```json
{
  "id": 1,
  "email": "test@example.com",
  "full_name": "Test User",
  "role": "owner",
  "business_id": 1,
  "is_active": true
}
```

### Успешный вход:

```json
{
  "access_token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "refresh_token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "token_type": "bearer"
}
```

### Список ботов (пустой):

```json
[]
```

### Ошибка без авторизации:

```json
{
  "detail": "Not authenticated"
}
```

## 🚨 Частые ошибки

### 401 Unauthorized

- Проблема: Токен не передан или невалиден
- Решение: Проверьте заголовок Authorization

### 404 Not Found

- Проблема: Ресурс не найден или не принадлежит вашему бизнесу
- Решение: Проверьте ID и права доступа

### 422 Validation Error

- Проблема: Неверный формат данных
- Решение: Проверьте схему данных в Swagger

## 📊 Мониторинг

### Логи Backend:

Смотрите в терминале где запущен Backend - все запросы логируются в реальном времени.

### Проверка здоровья API:

```bash
curl http://localhost:8000/api/v1/health
```

Ответ:

```json
{
  "status": "healthy",
  "version": "1.0.0",
  "environment": "development"
}
```

## 🎯 Следующие шаги

После успешного тестирования базовых endpoints:

1. Создайте WhatsApp номер (через UI или API)
2. Создайте бота
3. Постройте сценарий в Bot Builder
4. Подключите WhatsApp API
5. Протестируйте реальные сообщения
