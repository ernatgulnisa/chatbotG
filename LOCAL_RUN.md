# 🚀 Локальный запуск без Docker

## Вариант 1: Упрощенный запуск (БЕЗ PostgreSQL и Redis)

Для тестирования интерфейса можно запустить только Frontend:

### Шаг 1: Запустите Frontend

```powershell
cd frontend
npm install
npm run dev
```

Frontend будет доступен на http://localhost:5173

**Примечание:** Backend пока не будет работать, но вы сможете увидеть весь интерфейс.

---

## Вариант 2: Полный запуск с SQLite (РЕКОМЕНДУЕТСЯ для начала)

Давайте упростим Backend, чтобы использовать SQLite вместо PostgreSQL:

### Шаг 1: Настройте Backend для SQLite

1. Откройте файл `backend/app/core/config.py`
2. Найдите строку:

```python
DATABASE_URL: str = "postgresql://postgres:postgres@localhost:5432/chatbot_db"
```

3. Замените на:

```python
DATABASE_URL: str = "sqlite:///./chatbot.db"
```

### Шаг 2: Установите зависимости Backend

```powershell
cd backend
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
```

### Шаг 3: Создайте базу данных

```powershell
# Находясь в папке backend с активированным venv
python -c "from app.core.database import engine; from app.models import base; base.Base.metadata.create_all(bind=engine)"
```

### Шаг 4: Запустите Backend

```powershell
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Backend будет доступен на http://localhost:8000

### Шаг 5: Запустите Frontend (в новом терминале)

```powershell
cd frontend
npm install
npm run dev
```

Frontend будет доступен на http://localhost:5173

---

## Вариант 3: Установить Docker (для продакшена)

Если хотите использовать Docker:

### Windows:

1. Скачайте **Docker Desktop for Windows**: https://www.docker.com/products/docker-desktop
2. Установите и перезагрузите компьютер
3. Запустите Docker Desktop
4. Затем выполните:

```powershell
docker-compose up -d
```

---

## 🎯 Текущая ситуация

У вас есть **два варианта**:

### ✅ Быстрый старт (Вариант 1)

- Запустить только Frontend
- Посмотреть весь интерфейс
- **Время: 2 минуты**

### ✅ Полный функционал (Вариант 2)

- Backend + Frontend работают
- SQLite база данных (проще PostgreSQL)
- Авторизация работает
- **Время: 10 минут**

### ⏳ Продакшн версия (Вариант 3)

- Установить Docker
- Полная настройка с PostgreSQL и Redis
- **Время: 30 минут**

---

## 🔧 Текущая проблема и решение

**Проблема:** Docker не установлен на вашем компьютере

**Решение:** Используйте **Вариант 2** (SQLite + локальный запуск)

Это самый простой способ начать работу прямо сейчас!

---

## 📝 Пошаговая инструкция (Вариант 2)

Давайте запустим проект прямо сейчас. Выполните команды по порядку:
