# 🤖 Настройка Автоответчика Бота

## ✅ Что сделано

### 1. Создан готовый шаблон бота для салона красоты

Бот автоматически отвечает на сообщения в WhatsApp по ключевым словам:

#### 📋 Доступные команды:

| Ключевые слова | Ответ бота |
|----------------|------------|
| `1`, `стрижка`, `укладка`, `подстричься` | Информация о стрижках (от 1500₸) |
| `2`, `окрашивание`, `покрасить`, `цвет` | Информация об окрашивании (от 3000₸) |
| `3`, `маникюр`, `педикюр`, `ногти` | Маникюр и педикюр (от 2500₸) |
| `4`, `косметология`, `чистка`, `пилинг` | Косметология (от 4000₸) |
| `5`, `массаж`, `релакс`, `спа` | Массаж и SPA (от 3000₸) |
| `цены`, `прайс`, `стоимость` | Полный прайс-лист |
| `график`, `время`, `работаете` | График работы |
| `записаться`, `запись`, `бронь` | Форма записи |

### 2. Приветственное сообщение

При первом контакте клиент получает:
```
Здравствуйте! 👋 Добро пожаловать в наш салон красоты!

Я виртуальный ассистент, помогу вам записаться на услугу.

Выберите интересующую услугу:
1️⃣ Стрижка и укладка
2️⃣ Окрашивание
3️⃣ Маникюр/Педикюр
4️⃣ Косметология
5️⃣ Массаж

Напишите номер услуги или свой вопрос.
```

### 3. Ответ по умолчанию

Если ключевое слово не найдено, отправляется:
```
Спасибо за сообщение! ❤️

Для записи напишите:
• Желаемую услугу
• Удобную дату и время

Или выберите из меню:
1️⃣ Стрижка
2️⃣ Окрашивание
3️⃣ Маникюр
4️⃣ Косметология
5️⃣ Массаж
```

---

## 🚀 Как это работает на Render

### Автоматический деплой выполнен:
1. ✅ Добавлены поля в модель `BotScenario` (trigger_type, trigger_value, response_message, priority)
2. ✅ Обновлен `start-render.sh` для автоматической инициализации ботов
3. ✅ Создан скрипт `init_bot_templates.py` с готовыми сценариями
4. ✅ Исправлена обработка сообщений в `bot_processor.py`
5. ✅ Изменения отправлены на GitHub → Render автоматически деплоит

---

## 📱 Как протестировать

### На Render (Production):
1. Дождитесь завершения деплоя на https://dashboard.render.com/
2. Откройте https://chatbotg-web.onrender.com
3. Войдите в систему (21ernat@mail.ru)
4. Перейдите в **WhatsApp** → Добавить номер (если еще не добавлено):
   - Phone Number ID: `819213961283826`
   - WABA ID: `25435060679483721`
   - Access Token: (ваш токен из Meta)
5. Отправьте сообщение на ваш WhatsApp номер `+77711919140`:
   - Напишите: `Привет`
   - Напишите: `1` (стрижка)
   - Напишите: `цены`
   - Напишите: `записаться`

### Локально:
1. Бот уже создан локально (запущен `init_bot_templates.py`)
2. Можно тестировать через:
   ```powershell
   cd backend
   & "C:/Program Files/Python311/python.exe" test_bot_responses.py
   ```

---

## 🛠️ Настройка бота

### Через веб-интерфейс:
1. Войдите на https://chatbotg-web.onrender.com
2. Перейдите в **Боты** → Салон красоты
3. Здесь вы можете:
   - Изменить приветственное сообщение
   - Добавить новые ключевые слова
   - Изменить ответы
   - Добавить новые сценарии

### Через базу данных:
Боты хранятся в таблицах:
- `bots` - основные настройки бота
- `bot_scenarios` - сценарии с ключевыми словами и ответами

---

## 🔄 Автообновление на Render

Каждый раз при `git push` на GitHub:
1. Render автоматически забирает новый код
2. Запускается `start-render.sh`
3. Применяются миграции (если есть)
4. Запускается `init_bot_templates.py` (создает ботов, если их нет)
5. Сервер перезапускается

---

## 📊 Мониторинг

### Проверьте логи на Render:
1. Откройте https://dashboard.render.com/
2. Выберите сервис `chatbotg-web`
3. Перейдите в **Logs**
4. Найдите строки:
   ```
   🤖 Initializing bot templates...
   ✅ Бот для салона красоты создан!
   ```

### Проверьте webhook:
1. Отправьте тестовое сообщение на WhatsApp
2. В логах должно появиться:
   ```
   [OK] Processing message: <ваш текст>
   [OK] Scenario matched: <название сценария>
   [OK] Bot response sent
   ```

---

## ⚙️ Дополнительные настройки

### Рабочие часы:
В настройках бота есть параметр `working_hours`:
```json
{
  "auto_reply": true,
  "working_hours": {
    "enabled": true,
    "monday": "09:00-20:00",
    "tuesday": "09:00-20:00",
    "wednesday": "09:00-20:00",
    "thursday": "09:00-20:00",
    "friday": "09:00-20:00",
    "saturday": "10:00-18:00",
    "sunday": "closed"
  },
  "off_hours_message": "Спасибо за обращение! 🌙..."
}
```

Вне рабочих часов бот отправит специальное сообщение.

### Добавить новый сценарий:
В `backend/init_bot_templates.py` добавьте новый объект `BotScenario`:
```python
BotScenario(
    bot_id=beauty_salon_bot.id,
    name="Новая услуга",
    trigger_type="keyword",
    trigger_value=json.dumps(["ключевое", "слово", "еще"]),
    response_message="Ваш ответ здесь!",
    is_active=True,
    priority=9
)
```

---

## 🎯 Следующие шаги

1. **Добавить WhatsApp номер на Render** (если еще не сделано)
2. **Протестировать живые сообщения**
3. **Настроить тексты под ваш бизнес**
4. **Добавить больше сценариев** (акции, отзывы, FAQ)
5. **Настроить передачу оператору** (human takeover) при сложных вопросах

---

## 📞 Поддержка

Если бот не отвечает:
1. Проверьте логи Render
2. Убедитесь что WhatsApp номер добавлен
3. Проверьте что Access Token актуален
4. Убедитесь что вебхук работает (webhook verified)

Логи можно смотреть в реальном времени:
```
https://dashboard.render.com/ → chatbotg-web → Logs
```
