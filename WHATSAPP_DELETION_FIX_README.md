# ВАЖНО: Инструкция по исправлению удаления номеров WhatsApp

## 🔴 Проблема

Не удается удалить действующий номер WhatsApp из системы.

## ✅ Решение

Я подготовил исправление, но для его применения нужна запущенная база данных PostgreSQL.

## 📋 Что было сделано

### 1. Обновлена модель данных

- **Файл:** `backend/app/models/whatsapp_number.py`
- **Изменение:** Добавлено каскадное удаление для связей `bots` и `conversations`
- **Эффект:** При удалении номера автоматически удалятся все связанные данные

### 2. Улучшен API endpoint

- **Файл:** `backend/app/api/v1/endpoints/whatsapp.py`
- **Изменения:**
  - Добавлена обработка ошибок с try-catch
  - Более информативные сообщения об ошибках
  - Rollback транзакции при ошибке

### 3. Улучшен интерфейс

- **Файл:** `frontend/src/pages/WhatsAppSettings.jsx`
- **Изменения:**
  - Подробное предупреждение перед удалением
  - Список того, что будет удалено:
    - Все боты, привязанные к номеру
    - Все разговоры через этот номер
    - Все рассылки с этого номера
  - Понятные сообщения об успехе/ошибке

### 4. Создана миграция базы данных

- **Файл:** `backend/apply_cascade_migration.py`
- **Назначение:** Применяет CASCADE DELETE для внешних ключей

### 5. Создан PowerShell скрипт

- **Файл:** `fix_whatsapp_deletion.ps1`
- **Назначение:** Автоматическое применение всех исправлений

## 🚀 Как применить исправление

### Вариант 1: Если PostgreSQL установлен локально

1. **Убедитесь, что PostgreSQL запущен:**

   ```powershell
   # Проверьте службу
   Get-Service -Name "*postgres*"

   # Если не запущена, запустите
   Start-Service postgresql-x64-[версия]
   ```

2. **Запустите скрипт исправления:**

   ```powershell
   .\fix_whatsapp_deletion.ps1
   ```

3. **Перезапустите приложение:**
   ```powershell
   .\start.ps1
   ```

### Вариант 2: Если PostgreSQL в Docker

1. **Запустите PostgreSQL контейнер:**

   ```powershell
   docker-compose up -d db
   # или
   docker compose up -d db
   ```

2. **Запустите скрипт исправления:**

   ```powershell
   .\fix_whatsapp_deletion.ps1
   ```

3. **Перезапустите приложение:**
   ```powershell
   .\start.ps1
   ```

### Вариант 3: Ручное применение миграции

Если автоматический скрипт не работает:

1. **Подключитесь к PostgreSQL:**

   ```powershell
   psql -U postgres -d chatbot_db
   ```

2. **Выполните SQL-команды:**

   ```sql
   -- Боты
   ALTER TABLE bots
   DROP CONSTRAINT IF EXISTS bots_whatsapp_number_id_fkey,
   ADD CONSTRAINT bots_whatsapp_number_id_fkey
   FOREIGN KEY (whatsapp_number_id)
   REFERENCES whatsapp_numbers(id)
   ON DELETE CASCADE;

   -- Разговоры
   ALTER TABLE conversations
   DROP CONSTRAINT IF EXISTS conversations_whatsapp_number_id_fkey,
   ADD CONSTRAINT conversations_whatsapp_number_id_fkey
   FOREIGN KEY (whatsapp_number_id)
   REFERENCES whatsapp_numbers(id)
   ON DELETE CASCADE;

   -- Рассылки
   ALTER TABLE broadcasts
   DROP CONSTRAINT IF EXISTS broadcasts_whatsapp_number_id_fkey,
   ADD CONSTRAINT broadcasts_whatsapp_number_id_fkey
   FOREIGN KEY (whatsapp_number_id)
   REFERENCES whatsapp_numbers(id)
   ON DELETE CASCADE;
   ```

3. **Выйдите из psql:**

   ```
   \q
   ```

4. **Перезапустите сервер:**
   ```powershell
   .\start.ps1
   ```

## 🎯 Как проверить, что все работает

1. Откройте приложение: `http://localhost:3001/whatsapp`
2. Нажмите кнопку удаления (корзина) у любого номера
3. Вы увидите предупреждение о том, что будет удалено
4. Подтвердите удаление
5. Номер и все связанные данные должны удалиться без ошибок

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### Что удаляется вместе с номером:

- ✅ **Все боты** этого номера (и их сценарии)
- ✅ **Все разговоры** через этот номер (и все сообщения)
- ✅ **Все рассылки** с этого номера (и все отправленные сообщения)

### Это необратимая операция!

После удаления номера восстановить данные будет невозможно.

### Рекомендации:

1. Перед удалением номера убедитесь, что он вам действительно не нужен
2. Сделайте резервную копию базы данных, если в ней важные данные
3. Если номер используется активно, лучше деактивируйте его вместо удаления

## 📊 Текущий статус базы данных

На данный момент база данных не запущена. Вам нужно:

1. **Установить PostgreSQL** (если еще не установлен):

   - Скачать с https://www.postgresql.org/download/windows/
   - Или использовать Docker

2. **Запустить базу данных**

3. **Применить миграцию** одним из способов выше

## 🆘 Если что-то не работает

### Ошибка: "relation does not exist"

- База данных не инициализирована
- Запустите: `cd backend && python init_db.py`

### Ошибка: "constraint does not exist"

- Таблицы еще не созданы или имеют другие названия ограничений
- Проверьте существующие ограничения:
  ```sql
  SELECT constraint_name
  FROM information_schema.table_constraints
  WHERE table_name = 'bots';
  ```

### Ошибка подключения к БД

- Проверьте файл `.env`, убедитесь что `DATABASE_URL` правильный
- Убедитесь что PostgreSQL запущен
- Проверьте логин/пароль

## 📚 Дополнительная документация

- **Подробное описание:** `FIX_WHATSAPP_DELETION.md`
- **Скрипт миграции:** `backend/apply_cascade_migration.py`
- **Alembic миграция:** `backend/alembic/versions/001_add_cascade_delete_whatsapp_number.py`

## ✨ Резюме

Все исправления готовы и ждут применения. Как только вы:

1. Запустите PostgreSQL
2. Примените миграцию (любым из способов выше)
3. Перезапустите сервер

Проблема с удалением номеров WhatsApp будет решена! 🎉
